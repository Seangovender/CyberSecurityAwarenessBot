<Window x:Class="CyberSecurityChatbot_Part2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="🛡️ Cybersecurity Awareness Assistant" Height="700" Width="900"
        Background="#0D1117" WindowStartupLocation="CenterScreen"
        MinWidth="800" MinHeight="650">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- ASCII Art with Enhanced Styling -->
        <Border Grid.Row="0" Background="#161B22" CornerRadius="10" Margin="0,0,0,10" Padding="12"
                BorderBrush="#30363D" BorderThickness="1">
            <Border Background="#0D1117" CornerRadius="6" Padding="10">
                <TextBlock FontFamily="Consolas" FontSize="12" Foreground="#58A6FF"
                           TextAlignment="Left" TextWrapping="NoWrap"
                           LineHeight="17" LineStackingStrategy="BlockLineHeight">
                    <Run Text="╔══════════════════════════════════════════════════════════════════╗" />
                    <LineBreak />
                    <Run Text="║   ____      _                              _ _                   ║" />
                    <LineBreak />
                    <Run Text="║  / ___|   _| |__   ___ _ __ ___  ___  ___ | (_)_ __              ║" />
                    <LineBreak />
                    <Run Text="║ | |  | | | | '_ \ / _ \ '__/ __|/ _ \/ _ \| | | '_ \             ║" />
                    <LineBreak />
                    <Run Text="║ | |__| |_| | |_) |  __/ |  \__ \  __/ (_) | | | | | |            ║" />
                    <LineBreak />
                    <Run Text="║  \____\__, |_.__/ \___|_|  |___/\___|\___/|_|_|_| |_|            ║" />
                    <LineBreak />
                    <Run Text="║       |___/                                                      ║" />
                    <LineBreak />
                    <Run Text="║        🛡️ CYBERSECURITY AWARENESS ASSISTANT  🛡️                 ║" />
                    <LineBreak />
                    <Run Text="╚══════════════════════════════════════════════════════════════════╝" />
                </TextBlock>
            </Border>
        </Border>

        <!-- Status Bar -->
        <Border Grid.Row="1" Background="#161B22" CornerRadius="8" Margin="0,0,0,10" Padding="8,5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock x:Name="StatusText" Grid.Column="0" Foreground="#8B949E" FontSize="12"
                           Text="🟢 Ready" />
                <TextBlock Grid.Column="1" Foreground="#8B949E" FontSize="12" TextAlignment="Right">
                    <Run Text="Topics: " Foreground="#58A6FF"/>
                    <Run Text="Phishing | Password | Browsing | Social Eng. | Scam | Privacy" Foreground="#8B949E"/>
                </TextBlock>
            </Grid>
        </Border>

        <!-- Chat History ListBox with Enhanced Styling -->
        <ListBox Grid.Row="2" x:Name="ChatHistory" Background="#0D1117" Foreground="#E6EDF3"
                 FontSize="14" BorderBrush="#30363D" BorderThickness="1" 
                 Margin="0,0,0,10" Padding="5">
            <ListBox.Resources>
                <Style TargetType="ListBoxItem">
                    <Setter Property="Background" Value="Transparent"/>
                    <Setter Property="BorderThickness" Value="0"/>
                    <Setter Property="Padding" Value="5,3"/>
                    <Setter Property="Margin" Value="0,1"/>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="6" Padding="8,5" Margin="0,2">
                        <Border.Style>
                            <Style TargetType="Border">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding StringFormat={}{0:6}}" Value="You:">
                                        <Setter Property="Background" Value="#1C2333"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding StringFormat={}{0:9}}" Value="Assistant:">
                                        <Setter Property="Background" Value="#161B22"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Border.Style>
                        <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,2"/>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Input Area with Enhanced Styling -->
        <Border Grid.Row="3" Background="#161B22" CornerRadius="10" Padding="12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="UserInputBox" Grid.Column="0" Background="#0D1117" Foreground="#E6EDF3"
                         BorderBrush="#30363D" BorderThickness="1" FontSize="14" Padding="10"
                         KeyDown="UserInputBox_KeyDown"
                         CaretBrush="#58A6FF">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="TextBox">
                                        <Border Background="{TemplateBinding Background}"
                                                BorderBrush="{TemplateBinding BorderBrush}"
                                                BorderThickness="{TemplateBinding BorderThickness}"
                                                CornerRadius="6">
                                            <ScrollViewer x:Name="PART_ContentHost" Margin="2"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </TextBox.Style>
                </TextBox>
                <Button x:Name="SendButton" Grid.Column="1" Content="📤 Send" Width="90" Margin="10,0,0,0"
                        Background="#238636" Foreground="White" FontWeight="SemiBold" FontSize="13"
                        Click="SendButton_Click" Cursor="Hand"
                        BorderThickness="0" Padding="10,8">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#238636"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border Background="{TemplateBinding Background}"
                                                CornerRadius="6" Padding="10,6">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                            <Style.Triggers>
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter Property="Background" Value="#2EA043"/>
                                </Trigger>
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter Property="Background" Value="#1E7A34"/>
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </Button.Style>
                </Button>
            </Grid>
        </Border>
    </Grid>
</Window>